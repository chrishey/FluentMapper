namespace Mapper.Console
{
    class Program
    {
        static void Main(string[] args)
        {
            // generate a random product

            // pass it to the lite mapper

            // show output

            // pass it through both mappers

            //show output
        }
    }
}
